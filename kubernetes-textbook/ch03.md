서비스(service) - 파드에서 들고나는 통신 트래픽의 라우팅을 맡는 리소스
어드레스 디스커버리(address discovery)

쿠버네티스가 만든 가상 네트워크는 클러스터 전체를 커버
서비스가 생성되면 서비스의 IP 주소가 클러스터 내 DNS 서버에 등록됨

서비스 유형
클러스터 IP - 파드가 어느 노드에 있더라도 접근 가능 / 클러스터 내에서만 유효

익스터널네임(ExternalName)

캐노니컬 네임(Canonical NAME, CNAME)

네임스페이스(namespace) - 다른 리소스를 하나로 묶기 위한 리소스